# Supabase 配置（可选，但多人共享 + 实时同步必须填）
# 在 https://supabase.com 注册 → 新建项目 → Project Settings → API 复制下面两个值
# 复制本文件为 .env 并填入，网站即自动切换为「云后端模式」（多人共享、实时同步）
# 留空或不填：网站以「本地模式」运行（照片用 data/photoList.ts，留言存本机浏览器）
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 共享家庭口令（可选）
# 填一个你们俩约定的口令（如 5201314），全站需先输入此口令才能进入（多人共用同一口令）。
# 留空 = 不启用口令门禁（本地开发 / 公网但不想加锁时）。
# 注意：Cloudflare Pages 等静态托管需在「环境变量」里也配置同名变量，.env 不会被打包上传。
VITE_APP_PASSCODE=
